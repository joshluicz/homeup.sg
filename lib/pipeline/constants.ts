/** Minimum LLM audit overall score (0–10) required before publish is allowed in the UI. */
export const PUBLISH_THRESHOLD = 8;

/** Minimum transaction rows per (town, property_type) cohort before stats are surfaced. */
export const MIN_SAMPLE = 5;
