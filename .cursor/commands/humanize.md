Read and follow the **humanise** skill at `~/.cursor/skills/humanise/SKILL.md`. Apply it completely.

## Task

Humanize the text in the user's message (everything after this command).

## Workflow

1. Read the skill file first.
2. Run the full process: identify AI patterns → draft rewrite → audit ("What makes this obviously AI generated?") → final rewrite.
3. Deliver: draft, audit bullets, final rewrite, and a short summary of changes.
4. Preserve meaning and paragraph structure. No em dashes or en dashes in the final rewrite.

## If no text was pasted

Ask the user to paste the text to humanize.

## Voice matching (optional)

If the user included a writing sample for voice calibration, match their style per the skill's Voice Calibration section.
